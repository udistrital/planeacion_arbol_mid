swagger: "2.0"
info:
  title: beego Test API
  description: beego has a very cool tools to autogenerate documents for your API
  version: 1.0.0
  termsOfService: http://beego.me/
  contact:
    email: astaxie@gmail.com
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
basePath: /v1
paths:
  /arbol/{id}:
    get:
      tags:
      - arbol
      description: Consulta el árbol por id
      operationId: ArbolController.ConsultarArbol
      parameters:
      - in: path
        name: id
        description: Id del árbol que quiere consultar
        required: true
        type: string
      responses:
        "200":
          description: ""
          schema:
            $ref: '#/definitions/models.Arbol'
        "403":
          description: :id is empty
  /arbol/nodo/{id}/activar:
    put:
      tags:
      - arbol
      description: Activa el nodo arbol
      operationId: ArbolController.ActivarNodo
      parameters:
      - in: path
        name: id
        description: Id del nodo que quiere activar
        required: true
        type: string
      responses:
        "200":
          description: '{string} delete success!'
        "403":
          description: id is empty
  /arbol/nodo/{id}/desactivar:
    delete:
      tags:
      - arbol
      description: Desactiva el nodo árbol
      operationId: ArbolController.DesactivarNodo
      parameters:
      - in: path
        name: id
        description: Id del nodo que quiere desactivar
        required: true
        type: string
      responses:
        "200":
          description: '{string} delete success!'
        "403":
          description: id is empty
  /arbol/plan/{id}/activar:
    put:
      tags:
      - arbol
      description: Activa el plan árbol
      operationId: ArbolController.ActivarPlan
      parameters:
      - in: path
        name: id
        description: Id del plan que quiere activar
        required: true
        type: string
      responses:
        "200":
          description: '{string} delete success!'
        "403":
          description: id is empty
  /arbol/plan/{id}/desactivar:
    delete:
      tags:
      - arbol
      description: Desactiva el plan árbol
      operationId: ArbolController.DesactivarPlan
      parameters:
      - in: path
        name: id
        description: Id del plan que quiere desactivar
        required: true
        type: string
      responses:
        "200":
          description: '{string} delete success!'
        "403":
          description: id is empty
definitions:
  models.Arbol:
    title: Arbol
    type: object
tags:
- name: arbol
  description: |
    ArbolController operations for Arbol
